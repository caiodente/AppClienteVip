<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="app.daazi.v1.appclientevip">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="View.linha_detalhe_cliente"></activity>
        <activity android:name="View.ConsultarClientesActivity" />
        <activity android:name="View.RecuperarSenhaActivity" />
        <activity android:name="View.NovoClienteVipActivity" />
        <activity android:name="View.ClientePessoaJuridicaActivity" />
        <activity android:name="View.ClientePessoaFisicaActivity" />
        <activity android:name="View.ClienteVip" />
        <activity android:name="View.LoginActivity" />
        <activity android:name="View.MainActivity" />
        <activity android:name="View.CredencialDeAcessoActivity" />
        <activity android:name="View.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>